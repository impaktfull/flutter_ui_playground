# 0.0.7

- Version bump to keep packages in sync

# 0.0.6

## Added

- Support for callback parameter types (e.g., `VoidCallback`, `ValueChanged<T>`)

# 0.0.5

## Added

- Support for `BorderRadius` and `BorderRadiusGeometry` parameter types

# 0.0.3 - 0.0.4

## Changed

- README now includes badge links to related packages

# 0.0.2

## Added

- Support for `List<T>` parameter types
- Support for `EdgeInsets`, `EdgeInsetsGeometry`, `ImpaktfullUiAsset`, and `Widget` parameter types
- Support for custom input classes via `customInputs` parameter
- Support for external components via `extraComponents` on `@UiPlaygroundComponents`
- `extraComponentsOnly` mode to only generate from explicit component configurations

# 0.0.1

Initial pre-release.
